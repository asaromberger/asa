<h1><%= @title %></h1>

<table>

<thead>
<tr>
<th>Account Group</th>
</tr>
</thead>

<tbody>
<% @rebalances.each do |group| %>
	<tr>
	<td><%= link_to group.rtype, finance_investments_rebalance_path(group.id), method: :get %></td>
	</tr>
<% end %>
</tbody>

</table>
